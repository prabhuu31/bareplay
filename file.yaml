---
   - hosts: localhost
     tasks:
      
       - name: Install packages 
         shell: npm install
         
         
       - name: Build
         shell: ng build
         
     
       - name: test
         shell: pm2 start "ng test"
         
       - name: run
         shell: pm2 start "ng serve"
            
...             

