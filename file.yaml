---
   - hosts: localhost
     tasks:
      
       - name: Install packages 
         shell: npm cache clean -f
                rm -rf node_modules
                npm i
         
         
         
       - name: Build
         shell: cd Documents/dashboard-monitor-master
                ls
                ng build
         
     
       - name: test
         shell: pm2 start "ng test"
         
       - name: run
         shell: pm2 start "ng serve"
            
...             

